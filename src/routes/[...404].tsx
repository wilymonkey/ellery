import { A } from "@solidjs/router";

export default function NotFound() {
  return (
    <main class="text-center p-4 flex items-center justify-center w-full">
      <h1 class="text-6xl font-bold uppercase font-alumni">
        404: Not Found
      </h1>
    </main>
  );
}
